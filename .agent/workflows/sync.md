---
description: 一键同步代码到远程仓库
---

# /sync 命令

当用户输入此命令时，请执行以下标准同步流程：

1. **状态检查**：运行 `git status` 确认当前更改。
2. **暂存文件**：执行 `git add .`。
3. **提交更改**：提示用户输入 commit message，或者根据当前更改自动生成一个简洁的提交记录。
4. **推送到远程**：执行 `git push origin [当前分支名]`。
5. **反馈确认**：告知用户同步结果及当前的远程分支状态。
